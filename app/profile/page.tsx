import ProfileClient from "./ProfileClient";

export const metadata = {
    title: "Profile | AIOT Study",
    description: "Check your study progress and achievements.",
};

export default function ProfilePage() {
    return <ProfileClient />;
}
